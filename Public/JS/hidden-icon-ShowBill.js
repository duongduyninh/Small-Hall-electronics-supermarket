document.addEventListener("DOMContentLoaded", function () {
    const icon = document.querySelector(".ShowBillDetail-icon");

    setTimeout(function () {
        icon.style.opacity = 1; // Sau 2 giây, hiển thị biểu tượng bằng cách đặt opacity thành 1
    }, 2000); // 2 giây
});
document.addEventListener("DOMContentLoaded", function () {
    const icon = document.querySelector(".ShowBillDetail-Content");

    setTimeout(function () {
        icon.style.opacity = 1; // Sau 2 giây, hiển thị biểu tượng bằng cách đặt opacity thành 1
    }, 2000); // 2 giây
});
